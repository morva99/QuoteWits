# QuoteWits 🎯

Веб-приложение для генерации цитат и шуток с возможностью сохранения в избранное. Full-stack приложение с архитектурой Client-Server.

## 📋 Описание

QuoteWits — это современное веб-приложение, которое позволяет пользователям:
- 🔍 Генерировать цитаты с фильтрацией по категориям, авторам и ключевым словам
- 😄 Получать случайные шутки
- ❤️ Сохранять понравившиеся цитаты и шутки в избранное
- 📱 Пользоваться приложением на любых устройствах (адаптивный дизайн)
- 🔐 Регистрироваться и входить в систему для доступа к личной коллекции

## 🚀 Технологии

### Frontend
- **React 18.3.1** — UI библиотека
- **Vite 5.1.0** — сборщик и dev-сервер
- **React Router DOM 7.9.5** — клиентский роутинг
- **Tailwind CSS** — utility-first CSS фреймворк
- **Axios 1.6.7** — HTTP клиент
- **Framer Motion 12.23.24** — библиотека анимаций
- **Sonner 2.0.7** — toast уведомления
- **Radix UI** — доступные UI компоненты
- **Lucide React** — иконки

### Backend
- **Node.js** — JavaScript runtime
- **Express 4.18.2** — веб-фреймворк
- **JWT 9.0.2** — аутентификация
- **bcryptjs 2.4.3** — хеширование паролей
- **CORS 2.8.5** — разрешение cross-origin запросов
- **dotenv 16.3.1** — переменные окружения


## 🛠️ Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm или yarn

### Установка зависимостей

**Backend:**
```bash
cd backend
npm install
```

**Frontend:**
```bash
cd frontend
npm install
```

### Запуск в режиме разработки

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
# или
npm start
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

После запуска:
- Frontend доступен по адресу: `http://localhost:3000`
- Backend API доступен по адресу: `http://localhost:5000`
- API проксируется через Vite: `http://localhost:3000/api/*` → `http://localhost:5000/api/*`

## 🔐 Аутентификация

Приложение использует JWT (JSON Web Token) для аутентификации:

1. **Регистрация/Вход:** Пользователь регистрируется или входит, получает JWT токен
2. **Хранение токена:** Токен сохраняется в `localStorage`
3. **Защищенные запросы:** Токен автоматически добавляется в заголовок `Authorization: Bearer <token>`
4. **Проверка токена:** При загрузке приложения токен проверяется через `/api/auth/verify`


## 👤 Автор: Кирилл Фролов

Проект создан в рамках учебного задания.
